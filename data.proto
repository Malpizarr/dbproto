
syntax = "proto3";

package data; // This is the protobuf package name, it doesn't have to change

option go_package = "./dbdata;dbdata";

message Record { map<string, string> fields = 1; }

message Records { map<string, Record> records = 1; }
